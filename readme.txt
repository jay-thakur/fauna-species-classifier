# Steps to run this code

## create an conda environment with command 
conda create -n ML -y
conda activate ML

# navigate to code folder

## then install the required libraries using command
pip3 install -r requirements.txt

## then run the python file using command 
python3 kmeans.py

You should see the outputs in console.